# Branches TODO

Simple client-side React app to manage branch names (Add/Edit/Remove/Filter) — stored in localStorage.

This repository was created locally. To push to GitHub, see instructions in the project root or use the GitHub CLI if available.
